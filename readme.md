# eslint config invintus

This is the shareable eslint config for projects using jsx.


## install

    $ npm install --save-dev eslint-config-invintus


Create a file called `.eslintrc` in your project root:

```js
{
    "extends": ["invintus"]
}
```
